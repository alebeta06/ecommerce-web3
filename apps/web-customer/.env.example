# web-customer — environment variables (template, NO secrets here)
# 🇪🇸 NOTA: Copia a `.env` y rellena. Todo público (lecturas on-chain + redirección a la pasarela).

NEXT_PUBLIC_RPC_URL=http://localhost:8545
NEXT_PUBLIC_ECOMMERCE_ADDRESS=
NEXT_PUBLIC_EURO_TOKEN_ADDRESS=
NEXT_PUBLIC_PAYMENT_GATEWAY_URL=http://localhost:3001   # where checkout redirects to pay
NEXT_PUBLIC_IPFS_GATEWAY=https://gateway.pinata.cloud/ipfs/   # read product images
