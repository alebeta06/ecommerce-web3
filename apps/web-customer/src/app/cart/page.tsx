// 🇪🇸 NOTA: placeholder. El carrito on-chain (getCart + ajuste de cantidades + checkout) se
// construye en una sesión posterior.
export default function CartPage() {
  return (
    <main className="mx-auto max-w-6xl p-8">
      <h1 className="text-2xl font-bold">Cart</h1>
      <p className="mt-2 text-muted">Coming soon — Cart</p>
    </main>
  );
}
