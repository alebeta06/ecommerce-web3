# payment-gateway — environment variables (template, NO secrets here)
# 🇪🇸 NOTA: Copia a `.env.local` y rellena. Todas las variables son NEXT_PUBLIC_* (públicas: solo
# lecturas/escrituras on-chain via MetaMask). La pasarela no tiene secretos de servidor ni IPFS.
# `.env.local` está gitignored.

# ── Blockchain (Anvil local) ────────────────────────────────────────────
NEXT_PUBLIC_RPC_URL=http://localhost:8545
NEXT_PUBLIC_CHAIN_ID=31337
# 🇪🇸 Addresses del despliegue. En Anvil (cuenta 0, nonces 0 y 1) son deterministas:
#   EuroToken = 0x5FbDB2315678afecb367f032d93F642f64180aa3
#   Ecommerce = 0xe7f1725E7734CE288F8367e1Bb143E90bb3F0512
NEXT_PUBLIC_EURO_TOKEN_ADDRESS=     # for approve() + balanceOf()
NEXT_PUBLIC_ECOMMERCE_ADDRESS=      # for processPayment()

# Nombre legible de la red esperada (se muestra en el guard de red). Fallback al chainId si falta.
# En Sepolia: "Ethereum Sepolia".
NEXT_PUBLIC_NETWORK_NAME=Anvil (local)
