// 🇪🇸 NOTA: Tailwind v4 se integra como plugin de PostCSS (@tailwindcss/postcss).
// No hace falta tailwind.config.js: la config es CSS-first (ver src/app/globals.css).
const config = {
  plugins: ["@tailwindcss/postcss"],
};

export default config;
