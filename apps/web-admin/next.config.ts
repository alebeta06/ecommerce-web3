import type { NextConfig } from "next";

// 🇪🇸 NOTA: configuración mínima de Next.js 15 (App Router). Por ahora sin opciones
// especiales; las features (rewrites, imágenes IPFS, etc.) se añadirán cuando hagan falta.
const nextConfig: NextConfig = {};

export default nextConfig;
